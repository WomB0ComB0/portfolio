/**
 * Shared middleware for admin API routes
 * Currently empty but can be extended with common middleware like:
 * - Rate limiting
 * - Request logging
 * - CORS configuration
 * - Error handling
 */

export {};
